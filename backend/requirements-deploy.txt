# =============================================================================
# AgriSense Backend Dependencies — RAG-Only Deployment
# =============================================================================
# Lightweight variant: excludes TensorFlow, ONNX, OpenCV (ML models)
# Keeps: FastAPI + RAG pipeline + Weather + CrewAI agent
# Expected Docker image: ~1.5GB (vs ~5GB full)
# =============================================================================

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Shared utilities
numpy>=1.24.0

# AI Agents & LLM
crewai>=0.28.0
langchain-groq>=0.0.1
litellm>=1.0.0
fastembed>=0.4.0

# RAG & Vector Store
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1
chromadb>=0.4.22
pypdf>=4.0.0
pyyaml>=6.0

# HTTP & Networking (weather service, health checks)
httpx>=0.25.0
requests>=2.31.0
websockets>=12.0

# Image handling (Pillow only — no OpenCV needed for RAG)
Pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
